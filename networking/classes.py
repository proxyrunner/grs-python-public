# Network Class 
# network-class.py

import netmiko

# Cisco parents class
class Network(object):
    def __init__(self, ip_addr):
            self.ip_addr = ip_addr
            self.hostname = None
            self.image_type = image_type

class ios(Network):
     
class asa(Network):

class wlc(Network):
        